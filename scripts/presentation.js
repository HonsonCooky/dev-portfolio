/**
 * Given the current user input - show some options available for tab auto complete.
 */
function showHint(){}

/**
 * Given some user input - update the visual position of the textbox caret.
 * @param {Event} ev 
 */
export function updateCaret(ev){}

/**
 * Given some user keyboard event - update the visual textbox on screen.
 * @param {KeyboardEvent} ev 
 */
export function updateTextbox(ev){}